{
  "name": "@relio/server",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "hono": "catalog:",
    "@hono/node-server": "^1.14.4",
    "@trpc/server": "catalog:",
    "@hono/trpc-server": "^0.4.0",
    "better-auth": "catalog:",
    "ai": "catalog:",
    "@ai-sdk/google": "catalog:",
    "dotenv": "catalog:",
    "zod": "catalog:",
    "@relio/api": "workspace:*",
    "@relio/auth": "workspace:*",
    "@relio/db": "workspace:*",
    "@relio/storage": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "tsx": "^4.19.2",
    "@types/node": "^22.13.11",
    "tsdown": "catalog:"
  }
}
